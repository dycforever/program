#probe kernel.function("*@mm/page_alloc.c") { 

probe begin
{
    printf("probe begin\n")
}


probe kernel.function("*") { 
    if (pid() == target())
        printf ("%ld   %s(%d) %s\n", target(), execname(), pid(), probefunc())
}